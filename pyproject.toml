[project]
name = "libraries-introduction"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Tezy Apps", email = "tezyapps@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "jupyter>=1.1.1",
    "matplotlib>=3.11.1",
    "numpy>=2.5.2",
    "pandas>=3.0.5",
    "seaborn>=0.13.2",
]

[project.scripts]
lesson = "libraries_introduction:main"
oops = "oops:main"
charts = "charts:main"

[build-system]
requires = ["uv_build>=0.12.4,<0.13.0"]
build-backend = "uv_build"
